name: Deploy React Portfolio (Staging & Prod)

on:
  push:
    branches:
      - "main"    # Trigger per Produzione
      - "staging" # Trigger per Staging

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      # 1. Checkout del codice
      - name: Checkout Code
        uses: actions/checkout@v4

      # 2. Setup Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20 # Sostituisci con la tua versione se necessario

      # 3. Installazione dipendenze
      - name: Install dependencies
        run: npm ci

      # 4. Build dell'Applicazione React (SOLUZIONE VITE)
      - name: Build React App (Vite Base Path)
        run: |
          if [ "${{ github.ref }}" == "refs/heads/staging" ]; then
            # Usa il flag --base corretto per Vite per definire la base path degli asset
            npm run build -- --base=/staging/ 
          else
            # Per Main, nessun flag è necessario (la base è la root)
            npm run build
          fi
        env:
          NODE_OPTIONS: --max_old_space_size=4096

      # 5. Deploy Cross-Repository
      - name: Deploy to User Pages Repository
        uses: peaceiris/actions-gh-pages@v3 
        with:
          # TOKEN: Utilizza il secret salvato
          personal_token: ${{ secrets.DEPLOY_TOKEN }} 
          
          # REPOSITORY DI DESTINAZIONE: Il tuo sito GitHub Pages
          external_repository: mannuccimattia/mannuccimattia.github.io 
          
          # BRANCH DI DESTINAZIONE: Il branch principale del repo di deploy
          publish_branch: main 
          
          # CARTELLA SORGENTE: L'output del build
          publish_dir: ./build 
          
          # LOGICA CONDIZIONALE PER STAGING/PRODUZIONE
          # Se il branch è 'staging', pubblica in mannuccimattia.github.io/staging/
          # Altrimenti, pubblica nella root (.).
          destination_dir: ${{ github.ref == 'refs/heads/staging' && 'staging' || '.' }}
          
          # Dettagli del commit
          force_orphan: true
          user_name: 'github-actions[bot]'
          user_email: 'github-actions[bot]@users.noreply.github.com'